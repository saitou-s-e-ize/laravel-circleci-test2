
version: 2.1
orbs:
  slack: circleci/slack@3.4.1
jobs:
  build:
    docker:
      - image: circleci/python
    steps:
      - slack/notify:
          color: '#42e2f4'
          message: "Hey ${CIRCLE_USERNAME}, ${CIRCLE_JOB} failed: ${CIRCLE_BUILD_URL}"
          webhook: 'https://hooks.slack.com/services/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' # webhook urlを入れてください。
      - run:
          command: |
            echo "hello world~"
            exit 1
workflows:
  version: 2
  btd:
    jobs:
    - build